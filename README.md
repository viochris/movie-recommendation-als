# movie-recommendation-als
A collaborative filtering movie recommender using ALS matrix factorization on Apache Spark (PySpark MLlib), trained on the MovieLens dataset. Companion project to an SVD-based recommender, exploring the same problem with a distributed training engine, with EDA, duplicate-ID cleaning, and cross-validated hyperparameter tuning.
